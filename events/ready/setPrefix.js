const GuildPrefix = require("../../Models/GuildPrefix.js");
const mongoose = require("mongoose");




module.exports = async (guild) =>{




    //console.log(`Joined the new Guild: ${guild.name} (id: ${guild.id})`);

    // const guildId = guild.id;
    // console.log(guildId)
    // try{
    //     if (foundGuild) {
    //         console.log(`Guild found on Database - setPrefix`);
    //         const prefix = foundGuild;
    //         console.log(prefix);
    //     } else {
    //         console.log(`Guild not found on Database - setPrefix`);
    //         console.log(foundGuild);
    //     }
    // } catch (e) {
    //     console.log(`Error: ${e}`);
    // }

};